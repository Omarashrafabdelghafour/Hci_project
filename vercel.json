{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "build": {
    "env": {
      "CI": "false"
    },
    "commands": [
      "npm install",
      "ng build --prod"
    ]
  }
}
